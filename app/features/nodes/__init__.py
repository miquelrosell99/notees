"""Nodes feature package.

The router and its helpers are intentionally not imported here to avoid
circular imports with other routers. Import them directly from
``app.features.nodes.router`` or ``app.features.nodes.router.helpers``.
"""
